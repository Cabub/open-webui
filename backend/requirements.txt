-r requirements-slim.txt

## Local inference stack (excluded from slim builds; inference is deferred
## to an external engine like ollama there)
transformers==5.5.4
sentence-transformers==5.5.1
accelerate==1.13.0
pyarrow==20.0.0 # fix: pin pyarrow version to 20 for rpi compatibility #15897
einops==0.8.2
sentencepiece==0.2.1
faster-whisper==1.2.1
av==14.0.1 # Caution: Set due to FATAL FIPS SELFTEST FAILURE, see discussion https://github.com/open-webui/open-webui/discussions/15720
colbert-ai==0.2.22
rapidocr==3.9.2

## Document processing extras (loaders/main.py has fallbacks when absent)
unstructured==0.22.31
python-docx==1.2.0 # unstructured's .doc/.docx path; slim's .docx route uses docx2txt
nltk==3.9.4
